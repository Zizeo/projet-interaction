version: "3.1"

rules:
  - rule: Boucler sur le combat
    condition:
      - slot_was_set:
          - combat_state: "ongoing"
    steps:
      - action: action_combat_turn
      - action: action_player_choice
